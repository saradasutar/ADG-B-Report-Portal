ADG(B) REPORT SUBMISSION PORTAL
NEW: a reliable way to verify you're on the latest version (index.html only)

===========================================================================
WHAT I FOUND
===========================================================================
The "FE v17.6.7" badge you've been checking against does NOT come from
index.html at all -- it's generated by a separate file, portal-v1767.js,
that I have never had access to view or edit anywhere in this
conversation. Every feature I've built has lived inside index.html's own
code, so that badge has likely shown the same number the entire time,
regardless of anything I've shipped -- it was never a reliable way to
tell whether you were on the latest file.

There was also a second static, unchanging marker in the footer
("Portal v17.6.10") with the same problem.

===========================================================================
THE FIX
===========================================================================
The footer now shows a build marker I control directly and update every
time I hand you a new index.html, shown alongside the LIVE backend
version this exact page load actually received:

    Portal file build: 17.7.23 · Live backend: v17.7.22

HOW TO USE THIS GOING FORWARD:
Whenever I say "I've updated index.html to build X", just look at the
bottom of the page. If it says build X, your browser has the file I
actually shipped. If it shows an older number, no code-page-source
searching or guessing needed -- just hard refresh
(Ctrl/Cmd+Shift+R) or reopen in a private window, exactly like before,
but now you'll know FOR CERTAIN when it's actually taken effect, and I
will always tell you the exact build number to look for.

This is separate from (and more reliable than) the "BE vX.X.X" badge
near Administrator, which already WAS accurate (it reads directly from
the live Apps Script deployment).

===========================================================================
DEPLOY
===========================================================================
1. Replace index.html in your GitHub Pages repo with the supplied
   index.html, commit changes (the green button), wait ~30-60s.
2. Code.gs does not need to be redeployed -- unchanged this round,
   still at backendVersion 17.7.22.
3. Hard refresh once (Ctrl/Cmd+Shift+R) or open in a private window.
4. Confirm the footer reads "Portal file build: 17.7.23" -- once it
   does, "🕘 History" and "📋 Mark physically received" will be visible
   in the Upload modal exactly as described before, since that code has
   been correctly in place since V17.7.21.
