ADG(B) REPORT PORTAL V16.5 — RAW DATA RUNTIME FIX

EXACT LIVE BUG FOUND
The Raw Data module called ensurePortalLiveDate(), but that function belongs to
a different private JavaScript IIFE. The browser threw ReferenceError before
bindRawEvents() ran. Result: Raw Data button was visible but had no working
click handler.

FIX
- Removed the cross-scope ensurePortalLiveDate() call from Raw Data module.
- Date/time remains managed by the login/header module.
- Raw Data event binding now completes.
- All V16.4 speed optimisations remain.
- No Workflow Details.
- Lazy Sticky Notes remains.
- Raw Data still uses one rawBootstrap request.

INSTALL
1. Replace raw-data.js.
2. Replace sticky-notes.js.
3. Replace Code.gs and deploy New version.
4. In index.html use EXACTLY:
   <script src="raw-data.js?v=16.5-runtimefix"></script>
   <script src="sticky-notes.js?v=16.5-runtimefix"></script>
5. Commit.
6. Open once with Ctrl+Shift+R.

Do NOT run setupPortal() or setPortalSecurityCodes().
Expected FE v16.5 | BE v16.5.
