ADG(B) REPORT PORTAL V16.8 — FAVICON POLISH

NEW
- Custom favicon matching the portal navy/teal/gold identity.
- SVG primary favicon plus PNG and ICO fallbacks.
- V16.7 Fast Data Engine retained.

GITHUB
1. Upload/replace raw-data.js and sticky-notes.js.
2. Upload favicon.svg, favicon-64.png and favicon.ico to repository root.
3. Add the favicon <link> lines from INDEX-CHANGE.txt inside <head>.
4. Use the V16.8 script lines at the bottom.
5. Commit and hard-refresh once.

APPS SCRIPT
1. Replace Code.gs only to show BE v16.8.
2. Deploy > Manage deployments > Edit > New version > Deploy.
3. Do NOT run setupPortal().
4. Do NOT run setPortalSecurityCodes().

EXPECTED
FE v16.8 | BE v16.8
